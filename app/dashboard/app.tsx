import React from 'react';
import Layout from './layout';

const App: React.FC = () => {
  return (
    <Layout children={undefined}>
      {/* Resto de tu aplicación */}
    </Layout>
  );
};

export default App;